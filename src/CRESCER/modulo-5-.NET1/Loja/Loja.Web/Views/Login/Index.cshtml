
@{
    ViewBag.Title = "Index";
}

<h2>Login</h2>

@using (Html.BeginForm("ListarProdutos", "Produto", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    @Html.TextBox("email", "", new { placeholder = "email" })
    @Html.Password("senha", "", new { placeholder = "senha" })

    <button type="submit">Entrar</button>
}
